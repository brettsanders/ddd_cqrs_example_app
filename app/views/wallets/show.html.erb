<%= link_to 'View All Wallets', wallets_path, class: "btn btn-default" %>
<h1>Manage your Wallet</h1>

<table class="table table-bordered">
  <tr>
    <td>UID</td>
    <td><%= @wallet.uid %></td>
  <tr>
  <tr>
    <td>Balance</td>
    <td><%= number_to_currency @wallet.balance %></td>
  <tr>
  <tr>
    <td>Transaction History</td>
    <td><%= link_to "View history", history_wallet_path(@wallet.uid) %></td>
  </tr>
  <tr>
    <td>
      Created By
    </td>
    <td>
      <%= @wallet.created_by || "Unknown" %>
    </td>
  </tr>
  <tr>
    <td>
      Deposit
    </td>
    <td>
      <%= form_tag({controller: "wallets", action: "add_money"}, method: "post") do %>
        Amount
        <%= number_field_tag(:amount_to_add, nil, placeholder: 'deposit amount') %><br>

        Description
        <%= text_field_tag(:description, nil, placeholder: "why..") %>

        <br>
        <%= submit_tag("Desposit Money", class: "btn btn-sm btn-primary") %>
      <% end %>
    </td>
  </tr>
<table>